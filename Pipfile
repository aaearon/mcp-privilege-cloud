[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
mcp = {extras = ["cli"], version = ">=1.0.0"}
httpx = ">=0.27.0"
python-dotenv = ">=1.0.0"
pydantic = ">=2.5.0"
pydantic-settings = ">=2.1.0"

[dev-packages]
pytest = ">=8.0.0"
pytest-asyncio = ">=0.23.0"
pytest-mock = ">=3.12.0"
pytest-cov = "*"

[requires]
python_version = "3.10"